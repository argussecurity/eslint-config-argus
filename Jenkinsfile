#!/usr/bin/env groovy

final NAME = 'eslint-config-argus'

basePipelineModule(name: NAME, baseDir: NAME, buildNumber: currentBuild.number, buildBody: {
    nodeExec(nodeVersion: '6', cmd: 'echo "no specific commands"')
})
